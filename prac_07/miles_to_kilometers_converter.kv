BoxLayout:
    orientation: 'vertical'
    BoxLayout:
        orientation: 'horizontal'
        TextInput:
            id: input_number
#            text: '0'
            on_text: app.calculate(input_number.text)
        BoxLayout:
            orientation: 'vertical'
            Button:
                text: 'Up'
                on_press: app.increment(input_number.text, 1)
            Button:
                text: 'Down'
                on_press: app.increment(input_number.text, -1)
#    Button:
#        text: 'Convert Miles to Kilometers'
#        on_press: app.calculate(input_number.text)
    Label:
        id: output_label
